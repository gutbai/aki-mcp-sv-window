let lastUrl=location.href;let prefilledForRoute=false;let scheduled=false;
function isNewChat(){return location.pathname==='/'}function getComposer(){return document.querySelector('#prompt-textarea')||document.querySelector('textarea[placeholder]')||document.querySelector('[contenteditable="true"][data-virtualkeyboard="true"]')}function composerText(element){return element instanceof HTMLTextAreaElement?element.value.trim():(element.innerText||element.textContent||'').trim()}function setComposerText(element,text){element.focus();if(element instanceof HTMLTextAreaElement){const setter=Object.getOwnPropertyDescriptor(HTMLTextAreaElement.prototype,'value')?.set;setter?setter.call(element,text):(element.value=text);element.dispatchEvent(new Event('input',{bubbles:true}));return}const selection=window.getSelection();const range=document.createRange();range.selectNodeContents(element);selection.removeAllRanges();selection.addRange(range);if(!document.execCommand('insertText',false,text)){element.textContent=text;element.dispatchEvent(new InputEvent('input',{bubbles:true,inputType:'insertText',data:text}))}}
async function prefill(){scheduled=false;if(!isNewChat()||prefilledForRoute)return;const composer=getComposer();if(!composer||composerText(composer))return;const{projects=[],defaultProjectId=null}=await chrome.storage.local.get(['projects','defaultProjectId']);const project=projects.find((item)=>item.id===defaultProjectId);if(!project?.path)return;setComposerText(composer,`@Aki\nProject base folder: ${project.path}\n\nTask: `);prefilledForRoute=true}function schedulePrefill(){if(scheduled)return;scheduled=true;setTimeout(prefill,120)}function detectRouteChange(){if(location.href!==lastUrl){lastUrl=location.href;prefilledForRoute=false}schedulePrefill()}
document.addEventListener('click',(event)=>{const newChatControl=event.target.closest('a[href="/"], [data-testid="create-new-chat-button"]');if(!newChatControl)return;prefilledForRoute=false;setTimeout(schedulePrefill,150)});chrome.storage.onChanged.addListener((changes,area)=>{if(area!=='local'||(!changes.projects&&!changes.defaultProjectId))return;prefilledForRoute=false;schedulePrefill()});new MutationObserver(detectRouteChange).observe(document.documentElement,{childList:true,subtree:true});schedulePrefill();
